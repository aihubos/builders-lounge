# 첫 화면 디자인 QA

## 비교 대상

- Source visual truth: `.superloopy/evidence/frontend/2026-08-14-simple-wave-hero/reference.png`
- Implementation screenshot: `output/playwright/simple-hero-1280-final.png`
- Combined comparison: `.superloopy/evidence/frontend/2026-08-14-simple-wave-hero/comparison-1280.png`
- State: 첫 진입, 밝은 테마, 등장 모션 완료, 메뉴 닫힘

## 정규화

- 원본: 1729×910px
- 구현 캡처: 1265×791px, CSS viewport 1280×800, device scale factor 1
- 비교 이미지: 원본을 1600×910으로 중심 크롭 후 1264×718로 축소, 구현은 고정 내비게이션 72px을 제외한 1264×718 영역 사용
- 브라우저 크롬은 제품 제약이므로 비교 영역에서 제외했다.

## 전체 화면 비교

- 비대칭 좌측 문구·우측 A 심볼 구도가 기준 이미지와 일치한다.
- 두 문장의 크기 차이, 네이비 색상, 넓은 흰 여백이 유지된다.
- 파도 방향과 하단 비중이 기준과 같은 흐름을 만든다.
- 실제 사용자 제공 A 심볼의 비율과 색상을 유지했다.

## 세부 비교

별도 확대 영역은 만들지 않았다. 기준과 구현을 합친 2528×718px 비교 이미지에서 문구, A 심볼 가장자리, 파도 질감을 모두 읽을 수 있어 전체 비교가 세부 확인에도 충분했다.

## 필수 품질 면

- Fonts and typography: 시스템 한글 글꼴, 700 굵기, 두 단계 크기, 모바일 의미 단위 줄바꿈 확인
- Spacing and layout rhythm: 데스크톱 2열, 모바일 단일 열, 20·32·48px 화면 여백 토큰 확인
- Colors and tokens: 네이비·화이트·아이스 블루만 사용, 새 값은 DESIGN.md 토큰에 기록
- Image quality and fidelity: 파도는 실제 래스터 배경, A는 제공 원본에서 투명 배경으로 후처리한 래스터 자산 사용
- Copy and content: `배우고, 공유하고, 성장한다.`와 `AI라는 큰 파도, 함께 탈 준비를 합니다.`를 그대로 사용

## 비교 이력

1. P1: 첫 구현의 A 심볼에 흰 사각형 배경이 보였다. 사용자 제공 심볼을 투명 PNG로 후처리해 해결했다.
2. P2: 390px에서 `공유하고`와 `함께`가 음절 중간에서 나뉘었다. 의미 단위 span과 모바일 줄바꿈으로 해결했다.
3. P2: 768px에서 A 심볼이 지나치게 작았다. 태블릿 전용 크기 토큰을 추가해 해결했다.

## 동작 확인

- 360·390·768·1280px에서 가로 스크롤 없음
- 모바일 메뉴 열기 및 Escape 닫기 정상
- 소개·교육 단계 앵커 이동 정상
- 이미지 로딩 정상
- 콘솔 오류와 경고 0건

## 남은 P3

- 생성한 파도 배경의 붓결은 원본과 픽셀 단위로 같지 않지만 방향, 밀도, 색감과 여백은 같은 디자인 언어를 유지한다.
- 정규화된 픽셀 비교 점수는 8점이다. 생성형 배경의 질감 차이가 대부분이며, 나란히 비교에서 핵심 구도와 메시지 위계는 기준과 일치한다.

## 성능 확인

- 모바일 Lighthouse 3회 중앙값: 성능 94, 접근성 100, 권장사항 100, SEO 100
- 데스크톱 Lighthouse 3회 중앙값: 전 항목 100
- CLS 0, TBT 0ms

final result: passed
